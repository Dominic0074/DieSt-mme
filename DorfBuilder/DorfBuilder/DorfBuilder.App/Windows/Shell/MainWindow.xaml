<Window x:Class="DorfBuilder.App.Windows.Shell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:toolbar="clr-namespace:DorfBuilder.App.Views.Toolbar"
        xmlns:sideBar="clr-namespace:DorfBuilder.App.Views.SideBar"
        xmlns:welcome="clr-namespace:DorfBuilder.App.Views.Welcome"
        xmlns:details="clr-namespace:DorfBuilder.App.Views.ItemDetails"
        xmlns:info="clr-namespace:DorfBuilder.App.Views.Info"
        mc:Ignorable="d"
        Title="DorfBuilder"
        Width="1000"
        Height="650"
        MinWidth="800"
        MinHeight="520"
        Background="{StaticResource AppBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <toolbar:ToolbarView Grid.Row="0"
                             Grid.ColumnSpan="2"
                             DataContext="{Binding}" />

        <sideBar:SideBarView Grid.Row="1"
                             Grid.Column="0"
                             DataContext="{Binding}" />

        <ContentControl Grid.Row="1"
                        Grid.Column="1"
                        Margin="24"
                        Content="{Binding ActivePage}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type welcome:WelcomeViewModel}">
                    <welcome:WelcomeView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type details:ItemDetailsViewModel}">
                    <details:ItemDetailsView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type info:InfoViewModel}">
                    <info:InfoView />
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</Window>

